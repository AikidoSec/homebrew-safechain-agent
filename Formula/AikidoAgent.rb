# typed: false
# frozen_string_literal: true

# This file was generated by the Homebrew formula generator
class AikidoAgent < Formula
  desc "Aikido Agent"
  homepage "https://github.com/AikidoSec/aikido-agent"
  version "0.1.0"
  license "AGPL"

  on_macos do
    if Hardware::CPU.intel?
      url "https://github.com/AikidoSec/aikido-agent/releases/download/v#{version}/aikido-agent-darwin-amd64"
      sha256 "913301163bcae37cd8eadf54e7afd8491b23ca909fc5583c914d922ed6f1627b"
    end
    if Hardware::CPU.arm?
      url "https://github.com/AikidoSec/aikido-agent/releases/download/v#{version}/aikido-agent-darwin-arm64"
      sha256 "af58a3238df3bd8059564ac3f3698e323f908831fd24b8f78bf72f44f1cc96d8"
    end
  end

  def install
    # Install Aikido Agent binary
    binary_name = Hardware::CPU.intel? ? "aikido-agent-darwin-amd64" : "aikido-agent-darwin-arm64"
    downloaded_file = if File.exist?(binary_name)
      binary_name
    elsif (file = Dir.glob("*").find { |f| File.file?(f) && File.executable?(f) })
      file
    else
      raise "Could not find downloaded binary file"
    end
    bin.install downloaded_file => "aikido-agent"
    chmod 0755, bin/"aikido-agent"
  end

  service do
    name macos: "com.aikidosecurity.aikidoagent"
    run [opt_bin/"AikidoAgent"]
    run_at_load true
    keep_alive true
    log_path var/"log/AikidoAgent.log"
    error_log_path var/"log/AikidoAgent.error.log"
  end

  test do
    system "#{bin}/AikidoAgent", "--version"
  end
end

